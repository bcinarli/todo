/**
 * @author Bilal Cinarli
 */

module.exports = {
    default: {
        description: 'Default tasks',
        tasks      : ['watch', 'handlebars', 'sass:dist', 'jshint', 'concat', 'uglify']
    }
};